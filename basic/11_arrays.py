# array = son estructuras de datos fundamental 
# Que permite agrupar valores

first_array = ['sacar la basura', 'pinarse', 'dormir', 'sacar la ropa']

# En python el primer elemento de un arreglo tiene posición(index) 0
print(first_array[0])
print(first_array[1])
print(first_array[2])
print(first_array[3])
#print(first_array[4])

# we can get an array large whit the funtion len
print(len(firts_array))

# Podemos agregar elementos al arreglo con la función append al final del array 
first_array.append('Comer')
first_array.append('Dormir')

first_array.insert(0, 'levantarse')
print(first_array)

# Además podemos agregar elemento al arreglo. 
